#include "main.h"

/**
 * add - adds two integers and returns the result
 * @n: - integer supplied in argument
 * @m: - integer supplied in argument
 * Return: the result of addition
 */

int add(int n, int m)
{
	int sum = n + m;

	return (sum);
}
