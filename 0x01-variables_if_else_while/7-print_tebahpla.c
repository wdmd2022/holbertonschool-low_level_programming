#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
	{
	int n;

	for (n = 122; n >= 97; n--)
		putchar(n);
	putchar(10);
	return (0);
}
