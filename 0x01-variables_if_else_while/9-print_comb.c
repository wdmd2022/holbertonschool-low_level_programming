#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
	{
	int n;

	n = 48;

	for (n = 48; n <= 57; n ++1)
		if (n != 57)
			putchar(n)
				putchar(44)
				putchar(32)
		else
			putchar(n)
	return (0);
}
